1) Open cmd at mywebsite location where manage.py file is available, then in cmd write command "python manage.py runserver" and if all the
   packages are installed then it will run successfully and the web page will open.

   if not, install python and set path of python and install django~=2.2
Django==2.2.24
pip==21.1.3
pytz==2021.3
setuptools==56.0.0
sqlparse==0.4.2
scikit-image==0.18.3
pandas==1.3.4
numpy==1.21.0

and then run the command "python manage.py runserver"


         (or)


2) open mywebsite.sln file with Visual Studio IDE and mywebsite project import it, then create new environment and install all the above mentioned 
packages and the browser will open on its own.

